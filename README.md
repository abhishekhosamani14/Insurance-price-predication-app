Create an folder in your Desktop
add the app.py,model.pkl files to that folder


open VSCode after that open the folder which you have created

run the below Hashtag commands in Terminal
create environment for windows
# python -m venv myenv
activate environment
# myenv\Scripts\activate
# pip install  streamlit pandas numpy seaborn matplotlib scikit-learn


After that run this command to view the app
# streamlit run app.py
